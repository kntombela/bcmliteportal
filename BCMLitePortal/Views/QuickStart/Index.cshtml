@{
    ViewBag.Title = "Index";
}

<!-- Progressbar -->
<ul id="progressbar">
    <li class="active">Personal Details</li>
    <li>Social Profiles</li>
    <li>Account Setup</li>
</ul>
<div class="alert alert-success hide"></div>

<!-- Form -->
@*@using (Html.BeginForm(null, null, FormMethod.Post, new {id = "regiration_form" }))
{
    @Html.AntiForgeryToken()

    <fieldset>
        @Html.Action("Organisation")
    </fieldset>
    <fieldset>
        @{Html.RenderAction("Plans");}  
    </fieldset>
    <fieldset>
        <h2>Step 3: Contact Information</h2>
        <div class="form-group">
            <label for="mob">Mobile Number</label>
            <input type="text" class="form-control" id="mob" placeholder="Mobile Number">
        </div>
        <div class="form-group">
            <label for="address">Address</label>
            <textarea class="form-control" name="address" placeholder="Communication Address"></textarea>
        </div>
        <input type="button" name="previous" class="previous action-button-previous" value="Previous" />
        <input type="submit" name="submit" class="submit action-button" value="Submit" />
    </fieldset>
            }*@


<div class="row" id="regiration_form">

        <div>
            @Html.Action("OrganisationDetails")
        </div>
        <div>
            @{Html.RenderAction("PlanDetails");}
        </div>

    </div>

@section scripts {

    @Scripts.Render("~/Scripts/mutlistep-form.js")

}